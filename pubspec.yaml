name: s_sidebar
description: A beautiful, customizable, and responsive sidebar widget for Flutter with smooth animations, badge support, and adaptive layouts.
issue_tracker: https://github.com/SoundSliced/s_sidebar/issues
repository: https://github.com/SoundSliced/s_sidebar
version: 2.0.0
homepage: https://github.com/SoundSliced/s_sidebar

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  s_packages: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
